{
  "name": "promise-lesson",
  "version": "1.0.0",
  "description": "promise-lesson",
  "author": "kanemototomoki",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "run-s generate && gh-pages -d dist"
  },
  "dependencies": {
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "gh-pages": "^3.0.0",
    "npm-run-all": "^4.1.5"
  }
}
